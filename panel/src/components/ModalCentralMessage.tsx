export default function ModalCentralMessage({ children }: { children: React.ReactNode }) {
    return <div className="p1 text-muted-foreground flex min-h-66 items-center justify-center text-xl">{children}</div>;
}
